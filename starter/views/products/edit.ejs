<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Product</title>
</head>
<body>
    <h1>Edit Product</h1>
    <form action="/product/<%=product._id%>?_method=put" method="POST">
      <p>
        <label for="name">Enter Product Name: </label>
        <input type="text" id="name" name="name" placeholder="Product Name" value="<%=product.name%>" />
      </p>
      <p>
        <label for="price">Enter Price: </label>
        <input type="number" id="price" name="price" placeholder="Price" value=<%=product.price%> />
      </p>
      <p>
        <label for="category_select">Select Category: </label>
        <select name="category" id="category_select">
          <% for(let i = 0; i < categories.length; i++) { %>
          <option value=<%= categories[i]%> <% if(categories[i] == product.category) {%> selected <% } %> > <%=categories[i]%></option>
          <% } %>
        </select>
      </p>
      <button type="submit">Submit</button>
      <a href="/product/<%=product._id%> ">Cancel</a>
</body>
</html>